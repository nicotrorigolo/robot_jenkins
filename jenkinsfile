pipeline {
    agent any
    stages {
        stage('Test') {
            steps{
                sh 'docker run -v ${PWD}/tests:/opt/robotframework/tests:Z -e BROWSER=chrome ppodgorsek/robot-framework:latest'
            }
        }
    }
}
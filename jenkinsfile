pipeline {
    node any
    stages {
        stage('build') {
            steps {
                sh 'docker pull mkbcrew/rbfwk'
                sh 'docker run mkbcrew/rbfwk'
                sh 'python --version'
            }
        }
    }
}